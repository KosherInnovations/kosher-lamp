// import gsap from "gsap";

// const AnimateModal = (infoHeader) => {
//   const tl = gsap.timeline();
//   tl.fromTo(infoHeader.current, 1.5, { x: 100, scale: 0 }, { x: 0, scale: 1 });
//   console.log("Modal Opened")
// };

// export default AnimateModal;
